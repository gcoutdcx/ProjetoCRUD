@model List<ProjetoCRUD.Models.Usuario>
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title></title>
    <link type="text/css" rel="stylesheet" href="~/assets/css/bootstrap.min.css" />

   
</head>
<body>
    <div><p><h1>Cadastro de Usuários</h1></p></div>

    <div>
        
        <div id="vao"></div>

        <div id="modalCriar" class="modal fade" tabindex="-1" role="dialog">
            <div class="modal-dialog" role="document">
                <form id="formCriar">
                    <div class="modal-content">
                        <div class="modal-header">
                            <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                            <h4 class="modal-title">Criar Usuario</h4>
                        </div>
                        <div class="modal-body">
                            <div class="form-group">
                                <label for="Nome">Nome</label>
                                <input type="text" class="form-control" name="Nome" id="txtNome" placeholder="Nome">
                            </div>
                            <div class="form-group">
                                <label for="Email">E-mail</label>
                                <input type="email" class="form-control" nome="Email" id="txtEmail" placeholder="E-mail">
                            </div>
                            <div class="form-group">
                                <label for="combo">Foto</label>
                                <input type="file" id="txtFoto" name="Foto" accept="image/jpeg" />
                            </div>
                        </div>
                        <div class="modal-footer">
                            <button type="submit" class="btn btn-primary">Criar</button>
                            <button type="button" class="btn btn-default" data-dismiss="modal">Fechar</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>


        <button class="btn btn-primary fa fa-pencil" type="button" onclick="mostrarModalCriar()">Novo Usuário</button>

        <table id="userTable" class="table">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Foto</th>
                    <th>Nome</th>
                    <th>Email</th>
                    <th>Editar</th>
                    <th>Excluir</th>
                </tr>
            </thead>

            <tbody>
                @for(int i = 0; i < Model.Count; i++)
                {
                    <tr>
                        <td>@Model[i].Id</td>
                        <td>FOTO</td>
                        <td>@Model[i].Nome</td>
                        <td>@Model[i].Email</td>
                        <td><button type="button" onclick="mostrarModalEditar(@Model[i].Id)">Editar</button></td>
                        <td><button type="button" onclick="(@Model[i].Id)">Excluir</button></td>
                    </tr>
                }
            </tbody>

        </table>

        <script type="text/javascript" src="~/assets/js/jquery-3.3.1.min.js"></script>
        <script type="text/javascript" src="~/assets/js/jquery.mask.min.js"></script>
        <script type="text/javascript" src="~/assets/js/jquery.validate.min.js"></script>
        <script type="text/javascript" src="~/assets/js/bootstrap.min.js"></script>
        <script type="text/javascript">

            $("#formCriar").validate({
                rules: {
                    Nome: {
                        required: true,
                        minlength: 5,
                        maxlength: 50
                    },

                    Email: {
                        required: true,
                        email: true
                    }
                },

                submitHandler: function (form) {
                    $.ajax({
                        url: "/Usuario/Criar",
                        method: "POST",
                        data: new FormData(document.getElementById("formCriar")),
                        contentType: false,
                        processData: false,
                        success: function (dados) {
                            if (dados == "ok") {
                                window.location.reload();
                            } else {
                                alert("Erro: " + dados);
                            }
                        },
                        error: function () {
                            alert("Erro");
                        }
                    });
                }
            });


            function mostrarModalCriar() {
                $("#modalCriar").modal({
                    backdrop: "static",
                    keyboard: false
                });

            }

            function mostrarModalEditar(id) {
                var obj = {
                    id: id
                };
                $.ajax({
                    url: "/Pedido/ModalEditar",
                    method: "GET",
                    cache: false,
                    data: obj,
                    success: function (dados) {
                        $("#vao").html(dados);

                        $("#modalEditar").modal({
                            backdrop: "static",
                            keyboard: false
                        });
                    },
                    error: function () {
                        alert("Erro");
                    }
                })
            }

        </script>
    </div>
</body>
</html>
